server {
    listen 80;
    server_name api.endyinboxes.com;

    # Redirect HTTP to HTTPS (after SSL is set up)
    # Uncomment after SSL certificate is installed
    # return 301 https://$server_name$request_uri;

    # For now, allow HTTP and proxy to backend
    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Increase timeouts for long-running requests
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://localhost:5000/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        access_log off;
    }
}

# HTTPS configuration (uncomment after SSL certificate is installed)
# server {
#     listen 443 ssl http2;
#     server_name api.endyinboxes.com;
#
#     ssl_certificate /etc/letsencrypt/live/api.endyinboxes.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.endyinboxes.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://localhost:5000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     location /health {
#         proxy_pass http://localhost:5000/health;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         access_log off;
#     }
# }

